const form = require('./form.jsonapi');
const option = require('./option.jsonapi');
const question = require('./question.jsonapi');
const validation = require('./validation.jsonapi');

module.exports = {
  form,
  option,
  question,
  validation
};
